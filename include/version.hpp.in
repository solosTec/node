#pragma once

//
//  @CAPITAL_NAME@_VERSION % 100 is the patch level
//  @CAPITAL_NAME@_VERSION / 100 % 1000 is the minor version
//  @CAPITAL_NAME@_VERSION / 100000 is the major version
//

#define @CAPITAL_NAME@_VERSION ${${PROJECT_NAME}_VERSION_NUMERAL}
#define @CAPITAL_NAME@_TIMESTAMP  ${VERSION_TIMESTAMP}
