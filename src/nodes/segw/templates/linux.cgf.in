#
#	startup configuration for smart energy gateway (segw)
#	created : ${_NOW_UTC}
#	platform: @CMAKE_SYSTEM@
#
[setup]
json = ${CLS_PATH_ETC}/smf/segw_v${${PROJECT_NAME}_VERSION_MAJOR}.${${PROJECT_NAME}_VERSION_MINOR}.json
pool-size = ${${PROJECT_NAME}_POOL_SIZE}
