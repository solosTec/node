#
#	startup configuration for modem node
#	created : ${_NOW_UTC}
#	platform: @CMAKE_SYSTEM@
#
[setup]
json = /etc/opt/smf/modem_v${${PROJECT_NAME}_VERSION_MAJOR}.${${PROJECT_NAME}_VERSION_MINOR}.json
pool-size = ${${PROJECT_NAME}_POOL_SIZE}
