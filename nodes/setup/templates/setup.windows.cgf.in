#
#	startup configuration for @PROJECT_NAME@ setup node
#	created : ${${PROJECT_NAME}_NOW_UTC}
#	platform: @CMAKE_SYSTEM@
#
[service]
enabled = false
name = node_setup_v${${PROJECT_NAME}_VERSION_MAJOR}

[setup]
json = $ENV{ProgramData}\solosTec\SMF\setup_v${${PROJECT_NAME}_VERSION_MAJOR}.${${PROJECT_NAME}_VERSION_MINOR}.json
pool-size = ${${PROJECT_NAME}_POOL_SIZE}
